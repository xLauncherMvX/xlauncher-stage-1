import React from 'react';
declare const LedgerConnect: ({ onClick, error, connectPageContent }: {
    onClick: () => void;
    error: string;
    connectPageContent?: React.ReactNode;
}) => JSX.Element;
export default LedgerConnect;
