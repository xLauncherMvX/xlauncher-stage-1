import { SmartContractResult } from 'types/transactions';
export declare function manageFailedTransactions({ results, hash, sessionId }: {
    results: SmartContractResult[];
    hash: string;
    sessionId: string;
}): void;
export default manageFailedTransactions;
