interface CalculateFeeLimitType {
    gasLimit: string;
    gasPrice: string;
    data: string;
    gasPerDataByte: string;
    gasPriceModifier: string;
    chainId: string;
    minGasLimit?: string;
    defaultGasPrice?: string;
}
export default function calculateFeeLimit({ minGasLimit, gasLimit, gasPrice, data: inputData, gasPerDataByte, gasPriceModifier, defaultGasPrice, chainId, }: CalculateFeeLimitType): string;
export {};
