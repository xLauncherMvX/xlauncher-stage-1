interface DenominateType {
    input: string;
    denomination: number;
    decimals: number;
    showLastNonZeroDecimal: boolean;
    addCommas?: boolean;
}
export default function denominate({ input, denomination, decimals, showLastNonZeroDecimal, addCommas, }: DenominateType): string;
export {};
